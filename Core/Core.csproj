<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
      <Folder Include="tools\" />
    </ItemGroup>

    <ItemGroup>
      <Compile Remove="Interfaces\IChatSessionClient .cs" />
      <Compile Remove="Interfaces\ICurrentUserState .cs" />
    </ItemGroup>

    <!-- 这三个文件依赖 Infrastructure（IDatabaseService / Infrastructure.Models），改由 Infrastructure.csproj 编译，避免循环依赖。 -->
    <ItemGroup>
      <Compile Remove="Events\ChatEvents.cs" />
      <Compile Remove="Interfaces\IMessageStore.cs" />
      <Compile Remove="Services\MessageStore.cs" />
      <Compile Remove="Services\ChatMessageCoordinator.cs" />
    </ItemGroup>

    <!-- UI/ViewModels should reference Core directly -->

</Project>
