# Chat_App agent 约定

改代码前先读相关实现、调用方、迁移和测试，确认当前语义与不变量；不要按类型名或表面相似度猜测。

优先级只有四项：正确与安全、可维护、可测量的性能、真实复用。共享只用于至少两个调用方共同拥有、稳定、线程安全且生命周期匹配的契约或资源；会话、流、`DbContext` 和可变状态不得跨所有者共享。

验证按聚焦单测/契约测试 → Release 构建 → 短时 smoke 推进；阶段长测和发布 soak 留到功能冻结后。下一阶段与接手状态见 `docs/NEXT-STAGE.md`。
